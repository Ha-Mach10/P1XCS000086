<Window
	x:Class="P1XCS000086.Views.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:core="clr-namespace:P1XCS000086.Core;assembly=P1XCS000086.Core"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
	xmlns:mdMetro="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:prism="http://prismlibrary.com/"
	Title="{Binding Title}"
	Width="1000"
	Height="1200"
	prism:ViewModelLocator.AutoWireViewModel="True">

	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/VS/Controls.xaml" />
				<ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/VS/Colors.xaml" />

				<ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ToggleButton.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Menu>
			<MenuItem Header="ファイル(_f)" />
		</Menu>

		<TabControl Grid.Row="1">
			<TabItem Style="{StaticResource MahApps.Styles.TabItem}">
				<TabItem.Header>
					<TextBlock Margin="8,0,8,0" Text="記番台帳" />
				</TabItem.Header>
				<Grid>
					<TabControl Style="{StaticResource MahApps.Styles.TabControl}">
						<TabItem Style="{StaticResource MahApps.Styles.TabItem}">
							<TabItem.Header>
								<TextBlock Margin="8,0,8,0" Text="ビューア" />
							</TabItem.Header>
						</TabItem>
						<TabItem Style="{StaticResource MahApps.Styles.TabItem}">
							<TabItem.Header>
								<TextBlock Margin="8,0,8,0" Text="登録" />
							</TabItem.Header>
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition />
								</Grid.ColumnDefinitions>
								<GroupBox Width="320" Padding="8">
									<GroupBox.Header>
										<TextBlock Text="記番登録処理" />
									</GroupBox.Header>
									<Grid>
										<Grid.RowDefinitions>
											<RowDefinition Height="Auto" />
											<RowDefinition />
										</Grid.RowDefinitions>
										<GroupBox>
											<GroupBox.Header>
												<TextBlock Text="登録種別" />
											</GroupBox.Header>
											<VirtualizingStackPanel Margin="8">
												<TextBlock Text="言語種別" />
												<ComboBox
													Margin="0,8,0,8"
													ItemsSource="{Binding LanguageItemCollection, Mode=OneWay}"
													SelectedValue="{Binding LanguageSelectedValue.Value, Mode=OneWayToSource}">
													<i:Interaction.Triggers>
														<i:EventTrigger EventName="SelectionChanged">
															<i:InvokeCommandAction Command="{Binding LanguageTypeComboChange}" />
														</i:EventTrigger>
													</i:Interaction.Triggers>
												</ComboBox>
												<TextBlock Text="言語種別" />
												<ComboBox
													Margin="0,8,0,8"
													ItemsSource="{Binding DevelopmentItemCollection, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
													SelectedIndex="{Binding DevItemSelectedIndex}"
													SelectedValue="{Binding DevelopmentSelectedValue.Value, Mode=OneWayToSource}">
													<i:Interaction.Triggers>
														<i:EventTrigger EventName="SelectionChanged">
															<i:InvokeCommandAction Command="{Binding DevelopmentTypeComboChange}" />
														</i:EventTrigger>
													</i:Interaction.Triggers>
												</ComboBox>
												<Border
													Width="180"
													Margin="0,8,0,8"
													Padding="8,6,8,6"
													HorizontalAlignment="Left"
													BorderBrush="White"
													BorderThickness="0.5"
													CornerRadius="5">
													<VirtualizingStackPanel Orientation="Horizontal">
														<TextBlock Text="レコード数 ：" />
														<TextBlock Text="{Binding RowsCount, Mode=OneWay}" />
													</VirtualizingStackPanel>
												</Border>
											</VirtualizingStackPanel>
										</GroupBox>
										<GroupBox Grid.Row="1" Margin="0,12,0,0">
											<GroupBox.Header>
												<TextBlock Text="記番登録処理" />
											</GroupBox.Header>
											<VirtualizingStackPanel Margin="8">
												<TextBlock Text="開発名称" />
												<TextBox Margin="0,8,0,8" Text="{Binding DevelopName}" />
												<TextBlock Text="コードネーム" />
												<TextBox Margin="0,8,0,8" Text="{Binding CodeName.Value}" />
												<TextBlock Text="使用用途" />
												<mah:ToggleSwitch
													Margin="8,0,0,0"
													Padding="0"
													Command="{Binding CheckedStateChanged}"
													Foreground="White"
													IsOn="{Binding UseApplicationIsChecked}"
													OffContent="自動"
													OnContent="手動" />
												<Grid Margin="0,0,0,8" Visibility="{Binding UseApplicationComboBoxVisibility}">
													<Grid.ColumnDefinitions>
														<ColumnDefinition />
														<ColumnDefinition Width="8" />
														<ColumnDefinition />
													</Grid.ColumnDefinitions>
													<ComboBox ItemsSource="{Binding UseApplication.Value}" SelectedValue="{Binding UseApplicationSelectedValue.Value}" />
													<ComboBox
														Grid.Column="2"
														ItemsSource="{Binding UseApplicationSub.Value}"
														SelectedValue="{Binding UseApplicationSubSelectedValue.Value}" />
												</Grid>
												<TextBox
													Margin="0,0,0,8"
													Text="{Binding UseApplicationManual}"
													Visibility="{Binding UseApplicationTextBoxVisibility}" />
												<TextBlock Text="流用番号" />
												<TextBox Margin="0,8,0,8" Text="{Binding ReferenceNumber}" />
												<TextBlock Text="旧番号" />
												<TextBox Margin="0,8,0,8" Text="{Binding OldNumber}" />
												<TextBlock Text="新番号" />
												<TextBox Margin="0,8,0,8" Text="{Binding NewNumber}" />
												<TextBlock Text="継承元番号" />
												<TextBox Margin="0,8,0,8" Text="{Binding InheritenceNumber}" />
												<TextBlock Text="説明" />
												<TextBox
													Height="60"
													Margin="0,8,0,8"
													VerticalContentAlignment="Top"
													AcceptsReturn="True"
													Text="{Binding Explanation.Value}"
													TextWrapping="Wrap" />
												<TextBlock Text="摘要" />
												<TextBox
													Height="60"
													Margin="0,8,0,8"
													VerticalContentAlignment="Top"
													AcceptsReturn="True"
													Text="{Binding Summary.Value}"
													TextWrapping="Wrap" />
												<Button Command="{Binding CodeNumberRegist}" Content="登録" />
											</VirtualizingStackPanel>
										</GroupBox>
									</Grid>
								</GroupBox>
								<DataGrid
									Grid.Column="1"
									CanUserAddRows="False"
									CanUserDeleteRows="False"
									CanUserResizeRows="False"
									GridLinesVisibility="All"
									ItemsSource="{Binding DataGridItems, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
									SelectionUnit="FullRow"
									VerticalScrollBarVisibility="Auto"
									VirtualizingPanel.IsVirtualizing="True"
									VirtualizingPanel.ScrollUnit="Pixel"
									VirtualizingPanel.VirtualizationMode="Recycling" />
							</Grid>
						</TabItem>
						<TabItem Style="{StaticResource MahApps.Styles.TabItem}">
							<TabItem.Header>
								<TextBlock Margin="8,0,8,0" Text="編集" />
							</TabItem.Header>
						</TabItem>
						<TabItem Style="{StaticResource MahApps.Styles.TabItem}">
							<TabItem.Header>
								<TextBlock Margin="8,0,8,0" Text="マスタ一覧" />
							</TabItem.Header>
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<GroupBox x:Name="DatabaseRegisterGroupBox" Width="200">
										<GroupBox.Header>
											<TextBlock Text="データベース接続" />
										</GroupBox.Header>
										<StackPanel Margin="8">
											<TextBlock Text="サーバー名" />
											<TextBox Margin="0,8,0,8" Text="{Binding Server.Value}" />
											<TextBlock Text="ユーザー名" />
											<TextBox Margin="0,8,0,8" Text="{Binding User.Value}" />
											<TextBlock Text="データベース名" />
											<TextBox Margin="0,8,0,8" Text="{Binding Database.Value}" />
											<TextBlock Text="パスワード名" />
											<TextBox Margin="0,8,0,8" Text="{Binding Password.Value}" />
											<CheckBox Margin="0,8,0,8" IsChecked="{Binding PersistSecurityInfo.Value}">
												<CheckBox.Content>
													<TextBlock Foreground="White" Text="Persist Security Info" />
												</CheckBox.Content>
											</CheckBox>
											<Button
												Margin="0,8,0,8"
												Command="{Binding RegistSqlConnectionString}"
												Content="登録" />
											<Button
												Margin="0,8,0,56"
												Command="{Binding SqlConnectionTest}"
												Content="接続テスト"
												Foreground="SpringGreen" />
											<mdMetro:Snackbar
												Width="{Binding ElementName=DatabaseRegisterGroupBox, Path=Width}"
												IsActive="{Binding SnackIsActive}"
												MessageQueue="{Binding SnackBarMessageQueue.Value}" />
										</StackPanel>
									</GroupBox>
								</StackPanel>
								<GroupBox Grid.Column="1" Padding="8">
									<GroupBox.Header>
										<TextBlock Text="マスタ" />
									</GroupBox.Header>
									<StackPanel Grid.Column="1">
										<StackPanel Orientation="Horizontal">
											<RadioButton Content="追加" />
											<RadioButton Margin="16,0,16,0" Content="編集" />
											<RadioButton Content="削除" />
										</StackPanel>
										<TextBlock Margin="0,8,0,8" Text="テーブル選択" />
										<ComboBox />
										<GroupBox Margin="0,8,0,0">
											<GroupBox.Header>
												<TextBlock Text="選択" />
											</GroupBox.Header>
											<ItemsControl ItemsSource="{Binding}">
												<ItemsControl.ItemsPanel>
													<ItemsPanelTemplate>
														<VirtualizingStackPanel />
													</ItemsPanelTemplate>
												</ItemsControl.ItemsPanel>
												<ItemsControl.ItemTemplate>
													<DataTemplate>
														<Grid>
															<Grid.ColumnDefinitions>
																<ColumnDefinition Width="100" />
																<ColumnDefinition Width="120" />
															</Grid.ColumnDefinitions>
															<TextBlock Text="" />
															<TextBox Grid.Column="1" Text="" />
														</Grid>
													</DataTemplate>
												</ItemsControl.ItemTemplate>
											</ItemsControl>
										</GroupBox>
										<DataGrid />
									</StackPanel>
								</GroupBox>
							</Grid>
						</TabItem>
					</TabControl>
				</Grid>
			</TabItem>
		</TabControl>
		<ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.ContentRegion}" />

		<StatusBar Grid.Row="2">
			<StatusBarItem Content="CodeManager" />
			<Separator Style="{StaticResource MahApps.Styles.Separator.StatusBar}" />
			<StatusBarItem Content="Selected Tab:" />
		</StatusBar>
	</Grid>
</Window>
